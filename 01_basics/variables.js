const accountId=12345 //constant

let email="pranav@pranav.com"
var pass="12345"
city="Jaipur" //variable can be declared without specifying

//var and let both are used to declare variable but var doesn't know about the scope of variable but let does, so we use let
let vari

console.log(accountId);
console.table([accountId, email, pass, city, vari]);
